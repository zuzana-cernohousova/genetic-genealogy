def lower_no_whitespace(string):
	return "".join(string.split()).lower()


def lower_one_space(string):
	return " ".join(string.split()).lower()


def one_space(string):
	return " ".join(string.split())
